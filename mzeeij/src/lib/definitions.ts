export type SimpleStats = {
  orderNum: number;
  returnNum: number;
  itemNum: number;
};

export type Counts = {
  count: number;
};

export type Revenue = {
  month: string;
  revenue: number;
};

export type BestSeller = {
  image: string;
};

export type AdminRow = {
  id: string;
  userId: string;
  accessLevel: string;
};
